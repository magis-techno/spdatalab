# ==============================================================================
# Spatial-Data-Lab 环境配置文件
# ==============================================================================
# 
# 使用方法：
# 1. 复制此文件为 .env: cp env.example .env
# 2. 修改下面的配置为你的实际值
# 3. 运行 make setup-fdw 设置FDW连接
# 
# 注意：.env文件包含敏感信息，不要提交到git仓库
# ==============================================================================

# PostgreSQL本地数据库配置
LOCAL_POSTGRES_HOST=local_pg
LOCAL_POSTGRES_PORT=5432
LOCAL_POSTGRES_DB=postgres
LOCAL_POSTGRES_USER=postgres
LOCAL_POSTGRES_PASSWORD=postgres

# FDW远程数据库配置 - 轨迹数据库
REMOTE_TRAJ_HOST=your_traj_host.example.com
REMOTE_TRAJ_DB=trajdb
REMOTE_TRAJ_PORT=5432

# FDW远程数据库配置 - 地图数据库  
REMOTE_MAP_HOST=your_map_host.example.com
REMOTE_MAP_DB=mapdb
REMOTE_MAP_PORT=5432

# FDW远程数据库认证
FDW_USER=your_remote_username
FDW_PASSWORD=your_remote_password

# Sprint 2 测试配置
TEST_DATASET_FILE=data/test_dataset.json
TEST_BATCH_SIZE=100
TEST_INSERT_BATCH_SIZE=50

# 开发模式配置
PYTHONPATH=src
SPDATALAB_LOG_LEVEL=INFO 